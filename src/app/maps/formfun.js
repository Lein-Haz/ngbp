/**
 * Created by Thisguy on 5/13/2015.
 */
angular.module('formFun',[
    'ui.router'
])
    .controller('formFunController',['$scope','formFactory','mapViewFactory',function($scope, formFactory, mapViewFactory){

    }])


    //For the form! Query item so that the map isn't moving around
;